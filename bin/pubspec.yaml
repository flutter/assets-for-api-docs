name: generator
description: Generator script for building API doc assets.

dependencies:
  animation_metadata:
    path: ../packages/animation_metadata
  archive: ^3.1.2
  process_runner: ">=4.0.0 <5.0.0"

dev_dependencies:
  analyzer: ^4.1.0
  test: ^1.16.8

environment:
  sdk: ">=2.17.0-0 <3.0.0"

