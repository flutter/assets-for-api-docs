name: diagram_capture
description: A package for capturing images of diagrams with Flutter.
repository: https://github.com/flutter/assets-for-api-docs/tree/master/packages/diagram_capture
issue_tracker: https://github.com/flutter/flutter/issues
version: 0.1.0
publish_to: none

environment:
  sdk: "^3.7.0-0"
  flutter: ">=1.0.0 <4.0.0"

dependencies:
  animation_metadata:
    path: ../animation_metadata
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  path: ^1.8.0
  path_provider: ^2.0.1
  vector_math: ^2.1.2

dev_dependencies:
  image: ^4.1.3

